apiVersion: v1
kind: List
items:
  - kind: BuildConfig
    apiVersion: build.openshift.io/v1
    metadata:
      name: kafka-listener-svc
      labels:
        app: openshift-skunkworks
        app-owner: openshift-skunkworks
        app.openshift.io/runtime: golang
    spec:
      runPolicy: Serial
      source:
        git:
          ref: develop
          uri: "https://github.com/redhat-partner-ecosystem/openshift-skunkworks"
        type: Git
      strategy:
        type: Source
        sourceStrategy:
          from:
            kind: ImageStreamTag
            name: "golang-base:1.16.12"
            #namespace: openshift
          env:
            - name: BUILD_DIR
              value: examples/kafka-listener
      output:
        to:
          kind: ImageStreamTag
          name: kafka-listener-svc:latest
      triggers:
        - type: GitHub
          github:
            secretReference:
              name: openshift-skunkworks-xops-secrets
        - type: ImageChange
          imageChange: {}
        - type: ConfigChange

  - kind: ImageStream
    apiVersion: image.openshift.io/v1
    metadata:
      name: kafka-listener-svc
      labels:
        svc: kafka-listener-svc
        app: openshift-skunkworks
        app-owner: openshift-skunkworks
        app.openshift.io/runtime: golang
      annotations:
        description: kafka-listener-svc services image